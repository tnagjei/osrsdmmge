# Cloudflare Pages 部署故障排除指南

## 快速诊断工具

### 1. 部署验证命令
```bash
# 运行本地构建和验证
npm run deploy:check

# 验证部署准备状态
npm run verify:deploy

# 检查构建日志
npm run build
```

### 2. 在线诊断工具
- [HTTP Status Checker](https://httpstatus.io/)
- [DNS Lookup](https://dnschecker.org/)
- [SSL Labs SSL Test](https://www.ssllabs.com/ssltest/)

## 常见问题及解决方案

### 🚫 构建失败

#### 问题1：依赖安装失败
```
Error: Cannot find module 'nunjucks'
```

**解决方案：**
```bash
# 清理npm缓存并重新安装
npm cache clean --force
npm install --registry=https://registry.npmmirror.com --fetch-timeout=600000 --fetch-retries=2

# 检查package-lock.json
rm package-lock.json
npm install
```

#### 问题2：Tailwind CSS 构建失败
```
Error: Cannot find module 'tailwindcss'
```

**解决方案：**
```bash
# 重新安装Tailwind CSS
npm install --save-dev tailwindcss@latest

# 检查Tailwind配置文件
ls -la tailwind.config.js
```

#### 问题3：页面构建脚本失败
```
Error: Missing layout data for locale: ar
```

**解决方案：**
```bash
# 检查layout-data.js文件
cat src/components/layout-data.js

# 验证多语言配置
grep -r "ar:" src/components/layout-data.js
```

### 🎨 样式问题

#### 问题1：CSS样式未加载
**现象：** 页面显示纯文本，无样式效果

**诊断步骤：**
1. 检查 `public/assets/site.css` 是否存在
2. 验证 HTML 中 CSS 引用路径
3. 检查 Tailwind 构建输出

**解决方案：**
```bash
# 重新构建Tailwind CSS
npm run tailwind:build

# 重新构建项目
npm run build

# 检查CSS文件大小
ls -lh public/assets/site.css
```

#### 问题2：字体显示异常
**现象：** 特殊字符或图标显示为方块

**解决方案：**
1. 检查字体文件是否存在
2. 验证 `@font-face` 声明
3. 考虑使用系统默认字体作为fallback

### 🌍 多语言问题

#### 问题1：语言切换不工作
**现象：** 点击语言链接无响应或跳转错误

**诊断步骤：**
1. 检查语言配置数据
2. 验证URL路由规则
3. 测试相对路径和绝对路径

**解决方案：**
```javascript
// 检查语言切换脚本
// 验证localeLabels对象
const localeLabels = {"en":"English","ja":"日本語","ko":"한국어","de":"Deutsch","es":"Español","ar":"العربية"};

// 测试URL生成逻辑
const targetHref = '/' + targetLocale + suffix;
```

#### 问题2：RTL布局问题
**现象：** 阿拉伯语页面布局混乱

**解决方案：**
1. 检查 `dir="rtl"` 属性设置
2. 验证CSS中的RTL样式
3. 测试阿拉伯语特定样式

### 🔗 路由和重定向问题

#### 问题1：404页面错误
**现象：** 某些页面返回404

**诊断步骤：**
1. 检查Vite路由配置
2. 验证构建输出文件
3. 测试本地开发服务器

**解决方案：**
```bash
# 检查构建输出
ls -la public/en/
ls -la public/ar/
ls -la public/ja/

# 验证路由配置
cat vite.config.js
```

#### 问题2：重定向循环
**现象：** 页面无限重定向

**解决方案：**
1. 检查重定向规则优先级
2. 验证正则表达式匹配
3. 测试具体的重定向路径

### ⚡ 性能问题

#### 问题1：页面加载缓慢
**诊断步骤：**
1. 检查文件压缩
2. 验证缓存头设置
3. 分析资源加载顺序

**解决方案：**
```bash
# 检查文件大小
find public -name "*.html" -exec ls -lh {} \;

# 验证gzip压缩
curl -H "Accept-Encoding: gzip" -I https://your-domain.com/
```

#### 问题2：图片加载失败
**解决方案：**
1. 检查图片文件路径
2. 验证图片格式支持
3. 考虑使用图片优化服务

### 🔒 SSL和安全问题

#### 问题1：混合内容警告
**现象：** 浏览器显示"不安全内容"警告

**解决方案：**
1. 检查所有资源使用HTTPS
2. 更新硬编码的HTTP链接
3. 配置内容安全策略

#### 问题2：SSL证书错误
**解决方案：**
1. 等待证书颁发完成
2. 检查DNS配置正确性
3. 联系Cloudflare支持

### 📱 移动端显示问题

#### 问题1：响应式布局失效
**诊断步骤：**
1. 检查viewport meta标签
2. 验证媒体查询
3. 测试不同设备尺寸

**解决方案：**
```html
<!-- 确保viewport正确设置 -->
<meta name="viewport" content="width=device-width, initial-scale=1">
```

### 🛠️ Cloudflare Pages 特定问题

#### 问题1：构建超时
**现象：** 部署时显示"Build timeout"

**解决方案：**
1. 优化构建脚本性能
2. 减少不必要的构建步骤
3. 考虑拆分构建任务

#### 问题2：环境变量未生效
**诊断步骤：**
1. 检查环境变量格式
2. 验证变量名大小写
3. 确认变量作用范围

**解决方案：**
```bash
# 本地测试环境变量
NODE_ENV=production npm run build

# 检查wrangler.toml配置
cat wrangler.toml
```

## 调试技巧

### 1. 本地调试
```bash
# 启动本地开发服务器
npm run dev

# 构建生产版本测试
npm run build && npm run preview
```

### 2. 网络调试
- 使用浏览器开发者工具的Network面板
- 检查资源加载状态和错误
- 分析请求头和响应头

### 3. 日志分析
- 检查Cloudflare Pages构建日志
- 查看函数执行日志
- 分析错误堆栈跟踪

## 寻求帮助

### 1. Cloudflare 官方资源
- [Cloudflare Pages 文档](https://developers.cloudflare.com/pages/)
- [Cloudflare 社区论坛](https://community.cloudflare.com/)
- [Cloudflare 状态页面](https://www.cloudflarestatus.com/)

### 2. 开发社区
- [Stack Overflow](https://stackoverflow.com/questions/tagged/cloudflare-pages)
- [GitHub Issues](https://github.com/cloudflare/pages-action/issues)
- [Discord 开发社区](https://discord.gg/cloudflaredev)

### 3. 专业支持
- Cloudflare 付费技术支持
- 认证的Cloudflare合作伙伴
- 自由职业者平台

## 预防措施

### 1. 部署前检查清单
- [ ] 运行 `npm run deploy:check`
- [ ] 测试所有语言版本
- [ ] 验证所有链接和资源
- [ ] 检查响应式设计
- [ ] 确认SEO元数据

### 2. 监控设置
- 配置错误跟踪（如Sentry）
- 设置性能监控
- 启用访问日志分析

### 3. 备份策略
- 保留最近几次的成功构建
- 备份重要配置文件
- 记录部署配置和环境变量

## 版本更新说明

- **v1.0** - 初始版本，包含基础故障排除指南
- **v1.1** - 添加多语言问题解决方案
- **v1.2** - 补充性能优化建议
- **v1.3** - 增加Cloudflare Pages特定问题

---

*最后更新：2025年9月28日*

*如有其他问题，请提交Issue或Pull Request更新此文档。*